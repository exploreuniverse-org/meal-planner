
  # MealPlanner

  This is a code bundle for MealPlanner. The original project is available at https://www.figma.com/design/MoNRlPi35vjOrjyZsnhIxT/MealPlanner.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  